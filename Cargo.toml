[package]
name = "rune"
version = "0.1.0"
edition = "2021"
repository = "https://github.com/fourdollars/rune"
homepage = "https://fourdollars.github.io/rune/"
license = "MIT"
description = "High-performance zero-trust AI Agent"

[dependencies]
rusqlite = { version = "0.35", features = ["bundled", "backup"], optional = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1"
clap = { version = "4", features = ["derive", "env"] }
colored = "3"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
anyhow = "1.0"
tokio = { version = "1", features = ["rt-multi-thread", "time", "process", "sync", "io-util", "io-std", "net", "macros", "signal"] }
indicatif = "0.18"
rustyline = "18"
libc = "0.2"
reqwest = { version = "0.13.3", default-features = false, features = ["json", "stream", "rustls-no-provider"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
futures = "0.3"
async-stream = "0.3"
tokio-stream = "0.1"
tokio-util = { version = "0.7", features = ["rt"] }
base64 = "0.23"
sha2 = "0.11.0"
hex = "0.4.3"
axum = { version = "0.8", optional = true }

[features]
default = []
notes = ["dep:axum", "dep:rusqlite"]

[profile.release]
opt-level = "z"       # Optimize for size
lto = true            # Link-Time Optimization (thin → full)
codegen-units = 1     # Single codegen unit for better optimization
panic = "abort"       # No unwinding tables
strip = true          # Strip symbols automatically

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
tempfile = "3"
